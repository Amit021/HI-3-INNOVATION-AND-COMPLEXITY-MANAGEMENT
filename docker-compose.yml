version: '3.8'

services:
  django:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - .:/workspaces/HI-3-INNOVATION-AND-COMPLEXITY-MANAGEMENT
    command: "./setup.sh"
